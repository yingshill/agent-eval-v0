# Upstream ALE tool — cloned separately, NOT re-hosted here
agents-last-exam/

# Secrets — NEVER commit
secret/
.env
*.env
gcp_key.json
*_key.json
*.pem
*-key.json

# Run outputs / logs / scratch
.logs/
*.log
output/
scratch/

# Local-only handoff (contains strategy context — never push)
HANDOFF.md

# OS / editor
.DS_Store
.idea/
.vscode/
__pycache__/

# Local Claude Code settings (machine-local permissions/state)
.claude/
